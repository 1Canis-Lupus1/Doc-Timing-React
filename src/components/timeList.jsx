import React from "react"

const TimeList=(props)=>{
    // {console.log(props)}
    return(
        // props.timing.map((val,index)=>{
        //     let timingStart=`timingStart-${index}`;
        //     let timingEnd=`timingEnd-${index}`;
        //     console.log(timingStart)
        // })
    )
        
}

export default TimeList;